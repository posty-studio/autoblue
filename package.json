{
	"name": "autoblue",
	"description": "Add Bluesky to your WordPress website. Automatically share new posts to Bluesky and integrate Bluesky replies with the comments on your website.",
	"version": "0.0.1",
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start",
		"plugin-zip": "composer install --no-dev && npm run build && node bin/plugin-zip && composer install"
	},
	"license": "GPL-3.0",
	"prettier": "@wordpress/prettier-config",
	"browserslist": [
		"extends @wordpress/browserslist-config"
	],
	"devDependencies": {
		"@wordpress/icons": "^10.13.0",
		"@wordpress/prettier-config": "^4.12.0",
		"@wordpress/scripts": "^30.5.1",
		"postcss-import": "^16.1.0",
		"postcss-preset-env": "^10.1.1",
		"prettier": "npm:wp-prettier@^3.0.3",
		"replace-in-file": "^8.2.0"
	},
	"dependencies": {
		"@wordpress/components": "^28.12.0"
	}
}
